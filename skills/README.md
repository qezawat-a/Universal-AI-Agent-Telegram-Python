# J-Rock skills directory
# Drop `*.md` files here: first `# Title` = skill name, rest = instructions.
# They are auto-loaded and can be activated via /skill use <name> or /skill import <file>.
# Example: `skills/translator.md`
